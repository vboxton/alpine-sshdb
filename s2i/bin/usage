#!/bin/bash -e

cat <<EOF
This is the vboxton/alpine-sshdb S2I image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build https://github.com/vboxton/alpine-sshdb.git --context-dir=test/test-app/ vboxton/alpine-sshdb sshdb

You can then run the resulting image via:
docker run -p 443:2222 sshdb
EOF
